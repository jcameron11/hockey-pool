# Olympics Hockey Pool Tracker (Auto ranks + auto medals) — Zero defaults

Missing players or placements now count as **0** instead of 999.

Score = (Team final placement × 5) + Forward 1 rank + Forward 2 rank + Defenseman rank
Lower total wins.
